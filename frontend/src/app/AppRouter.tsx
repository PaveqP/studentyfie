import {FC} from "react";
import {BrowserRouter, Route, Routes} from "react-router-dom";

const AppRouter: FC = () => {
    return (
        <BrowserRouter>
            <Routes>
            </Routes>
        </BrowserRouter>
    )
}

export default AppRouter;